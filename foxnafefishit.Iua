-- 🐟 Fish It • RNG Analyzer UI | Script Loader
-- by GPT-5 + Allexzzq

if not game:IsLoaded() then
	game.Loaded:Wait()
end

-- RAW SCRIPT ANALYZER
local SCRIPT_URL = "https://raw.githubusercontent.com/sadriehana-eng/aa/refs/heads/main/foxnafefishit.Iua"

-- ================= UI PANEL =================
local Players = game:GetService("Players")
local lp = Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "FishItRNG_UI"
gui.ResetOnSpawn = false
gui.Parent = lp:WaitForChild("PlayerGui")

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.fromScale(0.28, 0.22)
frame.Position = UDim2.fromScale(0.02, 0.6)
frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true

local corner = Instance.new("UICorner", frame)
corner.CornerRadius = UDim.new(0, 14)

local title = Instance.new("TextLabel", frame)
title.Size = UDim2.fromScale(1, 0.22)
title.BackgroundTransparency = 1
title.Text = "🎣 Fish It • RNG Analyzer"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextScaled = true

local info = Instance.new("TextLabel", frame)
info.Position = UDim2.fromScale(0, 0.25)
info.Size = UDim2.fromScale(1, 0.75)
info.BackgroundTransparency = 1
info.TextWrapped = true
info.TextYAlignment = Top
info.TextXAlignment = Left
info.Font = Enum.Font.Gotham
info.TextScaled = true
info.TextColor3 = Color3.fromRGB(200, 200, 200)
info.Text =
	"Status Pulau : UNKNOWN\n"..
	"Peluang Secret : ?\n"..
	"Cast Dianalisis : 0\n\n"..
	"Menunggu data..."

-- expose UI untuk script utama
_G.FishItRNG_UI = {
	Update = function(status, chance, cast, advice)
		info.Text =
			"Status Pulau : "..status..
			"\nPeluang Secret : "..chance..
			"\nCast Dianalisis : "..cast..
			"\n\n"..advice
	end
}

-- ================= LOAD SCRIPT =================
local success, err = pcall(function()
	loadstring(game:HttpGet(SCRIPT_URL, true))()
end)

if not success then
	info.Text = "GAGAL LOAD SCRIPT\nCek RAW URL"
	warn(err)
end
